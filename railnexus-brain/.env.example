# RailNexus Brain — Environment
# No secrets required for Brain. All config is injected via the Express backend.
# If running standalone (e.g. tests), no .env is needed.

# Optional: override port (default 8000)
# PORT=8000
